name: "03 - workflow runners"

on: push

jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest
    steps:
      - name: show OS
        run: |
          echo "I'm running on an Ubuntu runner."
          echo "Runner OS: $RUNNER_OS"
          